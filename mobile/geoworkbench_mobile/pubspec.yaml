name: geoworkbench_mobile
description: GeoWorkbench field capture demo app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  file_picker: ^8.1.2
  image_picker: ^1.1.2
  msal_auth: ^3.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/reliance-logo.png
    - assets/branding/reliance-roundel.png
    - assets/branding/simpro-logo.png
    - assets/msal_config.json
